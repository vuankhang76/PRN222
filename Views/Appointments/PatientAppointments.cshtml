@model IEnumerable<InfertilityApp.Models.Appointment>

@{
    ViewData["Title"] = $"Lịch hẹn - {ViewData["PatientName"]}";
}

<div class="flex justify-between items-center mb-6">
    <div>
        <h2 class="text-3xl font-bold text-gray-900">Lịch hẹn của @ViewData["PatientName"]</h2>
        <p class="text-gray-600">Mã bệnh nhân: #@ViewData["PatientId"]</p>
    </div>
    <div>
        <a asp-controller="Appointments" asp-action="Create" asp-route-patientId="@ViewData["PatientId"]" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors duration-200 flex items-center">
            <i class="bi bi-plus-circle mr-2"></i> Thêm mới
        </a>
    </div>
</div>

<div class="bg-white rounded-lg shadow-lg p-6">
    @if (Model.Any())
    {
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead>
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Bác sĩ</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Ngày</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Giờ</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Loại</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Trạng thái</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Mục đích</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"></th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    @foreach (var item in Model.OrderByDescending(a => a.AppointmentDate).ThenBy(a => a.AppointmentTime))
                    {
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap">@(item.Doctor?.FullName ?? "Chưa chỉ định")</td>
                            <td class="px-6 py-4 whitespace-nowrap">@item.AppointmentDate.ToString("dd/MM/yyyy")</td>
                            <td class="px-6 py-4 whitespace-nowrap">@item.AppointmentTime.ToString(@"hh\:mm")</td>
                            <td class="px-6 py-4 whitespace-nowrap">@item.AppointmentType</td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                @switch (item.Status)
                                {
                                    case "Scheduled":
                                        <span class="bg-blue-500 text-white px-2 py-1 rounded">Đã lên lịch</span>
                                        break;
                                    case "Completed":
                                        <span class="bg-green-500 text-white px-2 py-1 rounded">Hoàn thành</span>
                                        break;
                                    case "Cancelled":
                                        <span class="bg-red-500 text-white px-2 py-1 rounded">Đã hủy</span>
                                        break;
                                    case "Rescheduled":
                                        <span class="bg-yellow-500 text-white px-2 py-1 rounded">Đổi lịch</span>
                                        break;
                                    default:
                                        <span class="bg-gray-500 text-white px-2 py-1 rounded">@item.Status</span>
                                        break;
                                }
                            </td>
                            <td class="px-6 py-4">@item.Purpose</td>
                            <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                <a asp-action="Details" asp-route-id="@item.Id" class="text-blue-600 hover:text-blue-900 mr-3">
                                    <i class="bi bi-eye"></i> Chi tiết
                                </a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="text-blue-600 hover:text-blue-900">
                                    <i class="bi bi-pencil"></i> Sửa
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="text-center py-8">
            <i class="bi bi-calendar-x text-gray-400 text-5xl mb-4"></i>
            <p class="text-gray-600 mb-4">Chưa có lịch hẹn nào được ghi nhận</p>
            <a asp-controller="Appointments" asp-action="Create" asp-route-patientId="@ViewData["PatientId"]" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors duration-200 inline-flex items-center">
                <i class="bi bi-plus-circle mr-2"></i> Tạo lịch hẹn mới
            </a>
        </div>
    }
</div>

<div class="mt-6">
    <a asp-controller="Patients" asp-action="Details" asp-route-id="@ViewData["PatientId"]" class="text-gray-600 hover:text-gray-900 inline-flex items-center">
        <i class="bi bi-arrow-left mr-2"></i> Quay lại thông tin bệnh nhân
    </a>
</div>
